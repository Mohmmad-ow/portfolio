type Project = {
    title: string;
    description: string;
    link: string;
    technologies: string;
    image: string;
};

type Theme = "light" | "dark";
type Language = "en" | "ar";


export type { Project, Theme, Language };
